{
  "id": "sort-by.json",
  "description": "The definitions for common parameters rating to sorting search (and browse) results.",
  "definitions": {
    "sortBy": {
      "type": "array",
      "description": "An array of field names, prefixed with / for ascending order and \\ for descending order.",
      "items": {
        "type": "string"
      }
    }
  }
}