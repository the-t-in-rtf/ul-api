{
    "type": "object",
    "id": "message",
    "description": "The common format used for general confirmation and error messages.",
    "properties": {
        "isError": {
            "type": "boolean",
            "description": "Whether the request was completed successfully.  Set to 'true' if any problems occur."
        },
        "message": {
            "type": "string",
            "description": "An optional message describing the results of a REST call."
        }
    },
    "required": [
        "message"
    ]
}
