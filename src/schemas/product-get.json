{
    "type": "object",
    "id": "product-get.json",
    "definitions": {
        "source": {
            "type": "string",
            "minLength": 1,
            "errors": {
                "type": "You must enter a source to load a product record.",
                "minLength": "You must enter a source to load a product record."
            }
        },
        "sid": {
            "type": "string",
            "minLength": 1,
            "errors": {
                "type": "You must enter a record ID to load a product record.",
                "minLength": "You must enter a record ID to load a product record."
            }
        }
    },
    "properties": {
        "source": {
            "$ref": "#/definitions/source"
        },
        "sid": {
            "$ref": "#/definitions/sid"
        }
    },
    "required": ["source", "sid"],
    "errors": {
        "#/required/0": "The 'source' URL parameter is required.",
        "#/required/1": "The 'sid' URL parameter is required."
    }
}